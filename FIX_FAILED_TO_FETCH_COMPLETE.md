# ✅ Решение проблемы "Failed to fetch"

## Проблема

При вводе адреса сервера в приложении появляется ошибка:
```
Не удалось подключиться к серверу: Failed to fetch
```

## Что было исправлено

### 1. Улучшена обработка ошибок на фронтенде

**Файл:** `src/components/min/ServerSelectionPage.tsx`

**Изменения:**
- ✅ Добавлено логирование в консоль для отладки
- ✅ Увеличен таймаут с 5 до 10 секунд
- ✅ Добавлены более понятные сообщения об ошибках
- ✅ Добавлена обработка различных типов ошибок (CORS, timeout, network)
- ✅ Добавлены подсказки для пользователя

**Пример улучшенного сообщения:**
```
Не удалось подключиться к серверу. Возможные причины:
• Сервер не запущен
• Неверный адрес
• Проблемы с сетью
• CORS не настроен на сервере
```

### 2. Улучшен CORS middleware на сервере

**Файл:** `server/main.go`

**Изменения:**
- ✅ Добавлено логирование всех CORS запросов
- ✅ Улучшена обработка Origin заголовка
- ✅ Добавлен заголовок `Access-Control-Allow-Credentials`
- ✅ Добавлено логирование preflight запросов

**Пример логов:**
```
[CORS] GET /health from http://localhost:5173
[CORS] Preflight request from http://localhost:5173
```

### 3. Улучшен health endpoint

**Файл:** `server/main.go`

**Изменения:**
- ✅ Добавлено логирование health check запросов
- ✅ Добавлено поле `time` в ответ
- ✅ Улучшена обработка ошибок

**Пример ответа:**
```json
{
  "status": "ok",
  "service": "voicehub-server",
  "version": "2.0.0",
  "time": "2026-01-20T12:34:56Z"
}
```

### 4. Создана документация

**Файлы:**
- ✅ `FIX_FAILED_TO_FETCH.md` - краткая инструкция
- ✅ `TROUBLESHOOTING_CONNECTION.md` - полная инструкция по диагностике

## Как диагностировать проблему

### Шаг 1: Проверьте сервер

```bash
# На сервере
curl http://localhost:8080/health
```

Должно вернуть:
```json
{"status":"ok","service":"voicehub-server","version":"2.0.0","time":"..."}
```

### Шаг 2: Проверьте firewall

```bash
# На сервере
ufw allow 8080/tcp
ufw reload
```

### Шаг 3: Откройте консоль браузера

1. Нажмите F12 (DevTools)
2. Перейдите на вкладку Console
3. Попробуйте подключиться к серверу
4. Посмотрите на логи

Должны быть логи:
```
[ServerSelection] Checking server: http://your-server:8080/health
[ServerSelection] Response status: 200
[ServerSelection] Response data: {status: "ok", ...}
```

### Шаг 4: Проверьте логи сервера

```bash
# На сервере
journalctl -u voicehub -f
```

При подключении должны быть логи:
```
[CORS] GET /health from http://localhost:5173
[Health] Health check from 127.0.0.1:12345
```

## Частые причины и решения

### 1. Сервер не запущен

**Решение:**
```bash
systemctl start voicehub
```

### 2. Firewall блокирует порт

**Решение:**
```bash
ufw allow 8080/tcp
ufw reload
```

### 3. Неправильный URL

**Решение:**
- Убедитесь, что указан порт (по умолчанию 8080)
- Убедитесь, что указан протокол (http:// или https://)
- Проверьте, что IP/домен правильный

**Примеры правильных URL:**
```
http://localhost:8080
http://192.168.1.100:8080
http://45.67.89.123:8080
https://voicehub.example.com
```

### 4. CORS проблемы

**Решение:**
CORS уже настроен на сервере. Если проблема сохраняется, проверьте логи сервера.

### 5. Mixed Content (HTTPS → HTTP)

**Решение:**
- Используйте HTTPS для сервера
- Или откройте приложение по HTTP

## Быстрые команды

```bash
# Проверить сервер
curl http://localhost:8080/health

# Перезапустить сервер
systemctl restart voicehub

# Проверить логи
journalctl -u voicehub -f

# Открыть порт
ufw allow 8080/tcp

# Проверить порт
netstat -tuln | grep 8080
```

## Что изменилось для пользователя

### До:
```
❌ Непонятная ошибка "Failed to fetch"
❌ Нет подсказок что делать
❌ Нет логирования для отладки
```

### После:
```
✅ Понятные сообщения об ошибках
✅ Подсказки возможных причин
✅ Подробное логирование в консоли
✅ Подробная документация
```

## Итог

✅ Улучшена обработка ошибок на фронтенде  
✅ Улучшен CORS middleware на сервере  
✅ Улучшен health endpoint  
✅ Создана подробная документация  
✅ Добавлено логирование для отладки  

**Проект успешно собран и готов к использованию!** 🚀

## Документация

- **FIX_FAILED_TO_FETCH.md** - краткая инструкция
- **TROUBLESHOOTING_CONNECTION.md** - полная инструкция по диагностике
